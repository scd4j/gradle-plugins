def user = get("user_name")
def pass = get("user_password")

pre {
	// for now, in windows we are doing nothing
	useradd(user)
	passwd(user, pass)
}

post {
	// for now, in windows we are doing nothing
	userdel(user)
}