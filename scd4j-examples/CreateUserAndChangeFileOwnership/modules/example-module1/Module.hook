def user = get("wildfly_user")
def pass = get("wildfly_password")

pre {
	// for now, in windows we are doing nothing
	useradd(user)
	passwd(user, pass)
}

post {
	// for now, in windows we are doing nothing
	userdel(user)
}