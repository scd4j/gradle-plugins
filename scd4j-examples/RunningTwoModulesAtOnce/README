Follow the below steps to run this example:

Linux
	* First you must be logged in a Linux machine with the root user
	* Second, in the command line go to the 'RunningTwoModulesAtOnce' directory and type './gradlew'

Windows
	* First you must be logged in a Windows machine
	* Then, in the command line, go to the 'RunningTwoModulesAtOnce' directory and type 'gradlew'